const main = document.getElementById('main');
const searchTab = document.getElementById('searchTab');
const favTab = document.getElementById('favTab');
const favKey = 'fundFavV8';
const riskCacheKey = 'fundRiskV14';
let fav = JSON.parse(localStorage.getItem(favKey) || '[]');
let timer = null;
const esc = s => String(s ?? '').replace(/[&<>"']/g, x => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
function save(){localStorage.setItem(favKey,JSON.stringify(fav));}
function jsonp(url,cb,timeout=10000){return new Promise((ok,no)=>{const s=document.createElement('script');const t=setTimeout(()=>{cleanup();no(new Error('请求超时'));},timeout);function cleanup(){clearTimeout(t);s.remove();try{delete window[cb];}catch{}}window[cb]=d=>{cleanup();ok(d)};s.onerror=()=>{cleanup();no(new Error('数据源暂时无法访问'))};s.src=url;s.async=true;document.head.appendChild(s);});}
async function searchApi(q){const cb='fundSearch_'+Date.now();return jsonp('https://fundsuggest.eastmoney.com/FundSearch/api/FundSearchAPI.ashx?m=1&key='+encodeURIComponent(q)+'&callback='+cb,cb);}
function sourceUrls(code){return {holdings:'https://fundf10.eastmoney.com/ccmx_'+encodeURIComponent(code)+'.html',reports:'https://fundf10.eastmoney.com/jjgg_'+encodeURIComponent(code)+'_3.html',source:'https://fundf10.eastmoney.com/',csrc:'https://www.csrc.gov.cn/'};}
function openSource(url,title='来源页面'){const previous=main.innerHTML;main.innerHTML='<div class="source-view"><button class="back" id="sourceBack">‹ 返回</button><h2>'+esc(title)+'</h2><p>以下内容来自公开来源网站。</p><a class="source-button" href="'+esc(url)+'" target="_blank" rel="noopener">打开来源网站 ↗</a></div>';document.getElementById('sourceBack').onclick=()=>{main.innerHTML=previous;};}
function searchPage(){searchTab.classList.add('active');favTab.classList.remove('active');main.innerHTML='<section class="hero"><h1>查基金季报</h1><p>输入基金名称或 6 位基金代码</p></section><div class="searchbox"><span>⌕</span><input id="q" autocomplete="off" placeholder="例如：024239 或 华夏全球科技"></div><div class="hint">基金资料由来源网站提供。风险信息自动检索中国证监会公开记录。</div><div id="r" class="results"></div>';const q=document.getElementById('q');q.oninput=()=>{clearTimeout(timer);timer=setTimeout(()=>doSearch(q.value.trim()),300)};q.focus();}
async function doSearch(q){const r=document.getElementById('r');if(!q){r.innerHTML='';return;}r.innerHTML='<div class="loading">正在搜索……</div>';try{const d=await searchApi(q);const a=(d.Datas||[]).filter(x=>x.CODE);r.innerHTML=a.slice(0,15).map(x=>'<button class="card" data-code="'+esc(x.CODE)+'" data-name="'+esc(x.NAME)+'"><div class="card-main"><div class="name">'+esc(x.NAME)+'</div><div class="meta">'+esc(x.CODE)+' · '+esc(x.CATEGORYDESC||x.TYPE||'基金')+'</div></div><div class="chev">›</div></button>').join('')||'<div class="empty">没有找到基金</div>';[...r.querySelectorAll('.card')].forEach(b=>b.onclick=()=>openFund(b.dataset.code,b.dataset.name));}catch(e){r.innerHTML='<div class="error">搜索接口暂时无法访问，请稍后再试。</div>';}}
function favPage(){searchTab.classList.remove('active');favTab.classList.add('active');main.innerHTML='<section class="hero"><h1>我的自选</h1><p>自选保存在这台设备上</p></section>'+(fav.length?fav.map(x=>'<button class="card" data-code="'+esc(x.code)+'" data-name="'+esc(x.name)+'"><div class="card-main"><div class="name">'+esc(x.name)+'</div><div class="meta">'+esc(x.code)+'</div></div><div class="chev">›</div></button>').join(''):'<div class="empty">还没有自选基金<br><small>搜索基金后进入基金详情即可加入自选</small></div>');[...main.querySelectorAll('.card')].forEach(b=>b.onclick=()=>openFund(b.dataset.code,b.dataset.name));}
function riskCacheId(code){return riskCacheKey+'_'+String(code).replace(/\D/g,'').slice(0,6);}
function readRiskCache(code){try{const raw=localStorage.getItem(riskCacheId(code));if(!raw)return null;const x=JSON.parse(raw);return x&&x.data?x:null;}catch{return null;}}
function writeRiskCache(code,data){try{localStorage.setItem(riskCacheId(code),JSON.stringify({savedAt:Date.now(),data}));}catch{}}
async function fetchRiskApi(code,name,force=false){const u='/api/risk?code='+encodeURIComponent(code)+'&name='+encodeURIComponent(name)+(force?'&force=1':'');const r=await fetch(u,{headers:{Accept:'application/json','Cache-Control':'no-cache'}});if(!r.ok)throw new Error('风险接口 HTTP '+r.status);return await r.json();}
function riskPage(code,name){const u=sourceUrls(code);main.innerHTML='<div class="risk-head"><button class="back" id="riskBack">‹ 返回基金</button><div><div class="risk-title">历史风险</div><div class="risk-sub">V14.1 · 自动监管风险数据库</div></div><button class="risk-refresh" id="riskRefresh">↻</button></div><div class="risk-summary" id="riskSummary"><div class="risk-loading">正在自动检索证监会公开记录……</div></div><div class="risk-filter" id="riskFilter"><button class="on" data-type="全部">全部</button><button data-type="行政处罚">行政处罚</button><button data-type="市场禁入">市场禁入</button><button data-type="警示函">警示函</button><button data-type="监管措施">监管措施</button><button data-type="纪律处分">纪律处分</button><button data-type="诚信记录">诚信记录</button><button data-type="其他">其他</button></div><div id="riskList" class="risk-list"></div><div class="risk-note">风险记录根据基金管理人及公开监管信息自动整理。管理人被监管不等于本基金产品本身违法，风险评分仅用于信息整理。</div><div class="source">信息来源：<button class="plain-link" id="riskOfficial">中国证券监督管理委员会 ↗</button></div>';
let data={manager:'',records:[]},filter='全部',loading=false;
function renderSummary(fromCache=false,refreshing=false){const sum=document.getElementById('riskSummary'),records=data.records||[],counts=data.counts||{},score=data.riskScore?.score;let cls='risk-score-low';if(score!==null&&score!==undefined){if(score<50)cls='risk-score-high';else if(score<75)cls='risk-score-medium';}sum.innerHTML='<div class="risk-manager"><span>基金</span><b>'+esc(name)+'</b><em>'+esc(code)+'</em></div><div class="risk-manager"><span>管理人</span><b>'+esc(data.manager||'自动识别失败')+'</b></div>'+(data.managers?.length?'<div class="risk-manager"><span>基金经理</span><b>'+esc(data.managers.join('、'))+'</b></div>':'')+'<div class="risk-score-box"><div class="risk-score-main"><strong class="'+cls+'">'+(score==null?'--':score)+'</strong><span>风险指数</span></div><div class="risk-score-text"><b>'+esc(data.riskScore?.level||'未评估')+'</b><small>'+(score==null?'暂无足够数据':'分数越低代表历史监管记录越多')+'</small></div></div><div class="risk-stats"><div><strong>'+records.length+'</strong><small>关联记录</small></div><div><strong>'+(counts['行政处罚']||0)+'</strong><small>行政处罚</small></div><div><strong>'+(counts['警示函']||0)+'</strong><small>警示函</small></div><div><strong>'+(counts['监管措施']||0)+'</strong><small>监管措施</small></div></div>'+(data.latest?'<div class="risk-latest"><span>最近一次监管记录</span><b>'+esc(data.latest.date||'')+'</b><p>'+esc(data.latest.title||'')+'</p></div>':'')+'<div class="risk-status-line">'+(refreshing?'↻ 正在检查最新记录……':fromCache?'✓ 已从本机缓存快速加载':data.cached?'✓ Cloudflare 缓存结果':'✓ 刚刚完成公开信息检索')+'</div>';}
function render(){const list=document.getElementById('riskList'),arr=(data.records||[]).filter(x=>filter==='全部'||x.type===filter);if(!arr.length){list.innerHTML='<div class="risk-empty"><strong>'+(filter==='全部'?'未检索到匹配的公开监管记录':'该分类暂无记录')+'</strong><br><small>当前公开检索未找到匹配项，不代表绝对不存在其他记录。</small></div>';return;}list.innerHTML=arr.map(x=>'<article class="risk-item"><div class="risk-item-top"><span class="risk-tag '+((x.type==='行政处罚'||x.type==='市场禁入')?'danger':(x.type==='警示函'||x.type==='监管措施')?'warning':'')+'">'+esc(x.type||'其他')+'</span><time>'+esc(x.date||'日期未识别')+'</time></div><h3>'+esc(x.title||'证监会公开监管记录')+'</h3><div class="risk-meta-row"><span>'+esc(x.relation||'关联记录')+'</span></div><p><b>监管机关：</b>'+esc(x.agency||'中国证监会')+'</p><p><b>涉及对象：</b>'+esc(x.subject||data.manager||'未标明')+'</p>'+(x.summary?'<p>'+esc(x.summary)+'</p>':'')+(x.measure?'<div class="risk-result"><b>监管措施：</b>'+esc(x.measure)+'</div>':'')+'<button data-url="'+esc(x.url||u.csrc)+'" class="risk-detail">查看证监会原文 ↗</button></article>').join('');[...list.querySelectorAll('.risk-detail')].forEach(b=>b.onclick=()=>openSource(b.dataset.url,'证监会原文'));}
async function refresh(showLoading=true,force=false){if(loading)return;loading=true;if(showLoading)document.getElementById('riskSummary').innerHTML='<div class="risk-loading">正在检索证监会公开记录……<small>正在使用快速并行检索</small></div>';try{const result=await fetchRiskApi(code,name,force);if(result.ok===false)throw new Error(result.error||'风险检索失败');data=result;writeRiskCache(code,data);renderSummary(false,false);render();}catch(e){const cached=readRiskCache(code);if(data.records?.length){renderSummary(true,false);document.getElementById('riskSummary').insertAdjacentHTML('beforeend','<div class="risk-refresh-error">最新数据更新失败，当前显示已有结果。</div>');render();}else if(cached?.data){data=cached.data;renderSummary(true,false);render();}else{document.getElementById('riskSummary').innerHTML='<div class="risk-error"><b>自动风险检索暂时失败</b><br><small>'+esc(e.message||'网络错误')+'</small><br><button id="riskRetry" class="source-button">重新检索</button></div>';document.getElementById('riskRetry').onclick=()=>refresh(true,true);}}finally{loading=false;}}
const cached=readRiskCache(code);if(cached?.data){data=cached.data;renderSummary(true,false);render();setTimeout(()=>refresh(false,false),1000);}else refresh(true,false);
document.getElementById('riskBack').onclick=()=>openFund(code,name);document.getElementById('riskRefresh').onclick=()=>refresh(true,true);[...document.querySelectorAll('#riskFilter button')].forEach(b=>b.onclick=()=>{filter=b.dataset.type;document.querySelectorAll('#riskFilter button').forEach(x=>x.classList.remove('on'));b.classList.add('on');render();});document.getElementById('riskOfficial').onclick=()=>openSource(u.csrc,'中国证监会');}
function openFund(code,name){const is=fav.some(x=>x.code===code),u=sourceUrls(code);main.innerHTML='<div class="fund-head"><div class="backrow"><button class="back" id="back">‹ 返回</button></div><div class="fund-name">'+esc(name)+'</div><div class="code">'+esc(code)+'</div><button class="fav-button '+(is?'saved':'')+'" id="star"><span>'+(is?'★':'☆')+'</span> '+(is?'已收藏':'加入自选')+'</button></div><div class="fund-actions"><button id="holdings">重要持仓</button><button id="reports">历史季报</button><button id="risk">历史风险</button></div><div class="fund-source"><button id="source">信息来源 ↗</button></div>';document.getElementById('back').onclick=searchPage;document.getElementById('holdings').onclick=()=>openSource(u.holdings,'重要持仓');document.getElementById('reports').onclick=()=>openSource(u.reports,'历史季报');document.getElementById('risk').onclick=()=>riskPage(code,name);document.getElementById('source').onclick=()=>openSource(u.source,'东方财富基金资料');document.getElementById('star').onclick=()=>{if(fav.some(x=>x.code===code))fav=fav.filter(x=>x.code!==code);else fav.push({code,name});save();openFund(code,name);};}
searchTab.onclick=searchPage;favTab.onclick=favPage;searchPage();